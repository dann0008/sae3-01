{% extends 'base.html.twig' %}


{% block stylesheets %}
    <style>
        @import url(https://fonts.googleapis.com/css?family=Roboto:400,300,100,500,700,900);

        body {
            background: #6768bd;
        }
        h1 {
            color: #ffffff;
            padding-top: 40px;
            font-size: 30px;
            text-transform: uppercase;
            font-weight: 700;
            text-align: center;
        }
        .terForm{
            margin:auto;
            width: 70%;
        }
    </style>
{% endblock %}

{% block body %}
<h1>  Création d'un nouveau TER </h1>

    <div class="terForm">
    {{ form_start(form, {'method': 'POST'}) }}
    {{ form_row(form.NomSujet, {'label': 'Nom du Sujet'}) }}
    {{ form_row(form.Description, {'label': 'Description du sujet '}) }}
    <button type="submit" class="btn btn-primary">{{ label_submit }}</button>
    {{ form_end(form) }}
</div>


{% endblock %}