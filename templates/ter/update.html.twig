
{% extends 'base.html.twig' %}

{% block title %} Modification de {{ ter.nomSujet}} {% endblock %}
{% block stylesheets %}
    <style>
        @import url(https://fonts.googleapis.com/css?family=Roboto:400,300,100,500,700,900);

        body {
            background: #6768bd;
        }
        h1 {
            color: #ffffff;
            padding-top: 40px;
            font-size: 30px;
            text-transform: uppercase;
            font-weight: 700;
            text-align: center;
        }
        .terForm{
            margin:auto;
            width: 70%;
        }
    </style>
{% endblock %}
{% block body %}
    <h1> dddddddd</h1>
    <div class="terForm">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button id="btn">{{ button_label|default('Modifier') }}</button>
    {{ form_end(form) }}
    </div>{% endblock %}



